

    // add a text at position (100, 100)
    add([
        text("ohhimark", 32),
        pos(100, 100),
    ]);
    const download = add([
      sprite("download"),
      pos(50,50)
    ]);
    
    add([
      sprite("foo"),
      pos(50,50)
    ])
    add([
      sprite("testing"),
      pos(50,50)
    ])

